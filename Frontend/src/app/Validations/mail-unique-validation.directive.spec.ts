import { MailUniqueValidationDirective } from './mail-unique-validation.directive';

describe('MailUniqueValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new MailUniqueValidationDirective();
    expect(directive).toBeTruthy();
  });
});
